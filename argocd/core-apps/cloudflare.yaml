apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloudflare-config
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/jetri/gitops"
    path: manifests/cloudflare/overlay
    targetRevision: HEAD
  destination:
    server: "https://kubernetes.default.svc"
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
